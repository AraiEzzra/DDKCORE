# Transaction delegate asset

```json
{
    "username": "ddk_user"
}
```

## Verify

To verify `delegate asset`, you need to convert `username` to lowercase and check the absence in delegate
repository
